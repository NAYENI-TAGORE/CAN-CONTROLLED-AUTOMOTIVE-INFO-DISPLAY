//delay.h

#include"types.h"   // Includes custom data types like u32

// Function declarations (prototypes)

// Delay in microseconds
void delay_us(u32);   // Takes delay time in microseconds

// Delay in milliseconds
void delay_ms(u32);   // Takes delay time in milliseconds

// Delay in seconds
void delay_s(u32);    // Takes delay time in seconds
